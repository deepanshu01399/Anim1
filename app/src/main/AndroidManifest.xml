<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deepanshu.anim1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anim1">
        <activity android:name=".activity.BottomSheetActivity"></activity>
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SecondActivity" />
        <activity android:name=".activity.BasicAnim" />
        <activity android:name=".activity.ViewFlipper" />
        <activity android:name=".activity.ImageSLiderMover" />
        <activity
            android:name=".activity.SliderActivity"
            android:theme="@style/Theme.Anim1.SlidrAnimation" />
    </application>

</manifest>